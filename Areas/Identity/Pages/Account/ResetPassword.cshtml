@page
@model ResetPasswordModel
@{
    ViewData["Title"] = "Reset password";
}

<div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-5">
        <div class="glass-card shadow-lg mt-4">
            <div class="card-header-gradient text-center py-4">
                <div class="brand-icon-box mx-auto mb-3" style="width: 52px; height: 52px; font-size: 1.5rem;">
                    <i class="bi bi-shield-lock-fill"></i>
                </div>
                <h2 class="h4 fw-bold mb-1">Reset Password</h2>
                <p class="text-muted small mb-0">Create a secure, new password for your account.</p>
            </div>

            <div class="p-4 p-md-5">
                <form method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>
                    <input asp-for="Input.Code" type="hidden" />

                    <div class="form-floating-modern mb-3">
                        <label asp-for="Input.Email" class="form-label">
                            <i class="bi bi-envelope me-1 text-primary"></i> Email Address
                        </label>
                        <input asp-for="Input.Email" class="form-control form-control-modern" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        <span asp-validation-for="Input.Email" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="form-floating-modern mb-3">
                        <label asp-for="Input.Password" class="form-label">
                            <i class="bi bi-key me-1 text-primary"></i> New Password
                        </label>
                        <input asp-for="Input.Password" class="form-control form-control-modern" autocomplete="new-password" aria-required="true" placeholder="New password" />
                        <span asp-validation-for="Input.Password" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="form-floating-modern mb-4">
                        <label asp-for="Input.ConfirmPassword" class="form-label">
                            <i class="bi bi-check-circle me-1 text-primary"></i> Confirm Password
                        </label>
                        <input asp-for="Input.ConfirmPassword" class="form-control form-control-modern" autocomplete="new-password" aria-required="true" placeholder="Confirm password" />
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <button type="submit" class="btn btn-gradient-primary btn-shimmer w-100 py-3 mb-3">
                        <i class="bi bi-check2-circle me-2"></i> Update Password
                    </button>

                    <div class="text-center mt-3">
                        <a asp-page="./Login" class="text-decoration-none text-muted small">
                            <i class="bi bi-arrow-left me-1"></i> Back to Sign In
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
